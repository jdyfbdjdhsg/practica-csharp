<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!-- Анимация появления окна -->
	<Storyboard x:Key="WindowFadeIn">
		<DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1" Duration="0:0:0.5"/>
	</Storyboard>

	<!-- 1. Анимация появления схемы зала -->
	<Storyboard x:Key="SeatMapFadeInAnimation">
		<DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1" Duration="0:0:0.6"/>
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        From="0.8" To="1" Duration="0:0:0.5"/>
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        From="0.8" To="1" Duration="0:0:0.5"/>
	</Storyboard>

	<!-- 2. Анимация пульсации места при наведении -->
	<Storyboard x:Key="SeatPulseAnimation">
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        From="1.0" To="1.1" Duration="0:0:0.15" AutoReverse="True"/>
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        From="1.0" To="1.1" Duration="0:0:0.15" AutoReverse="True"/>
	</Storyboard>

	<!-- 3. МЯГКОЕ ИЗМЕНЕНИЕ ЦВЕТА ПРИ НАВЕДЕНИИ НА МЕСТО -->
	<Storyboard x:Key="SeatHoverColorAnimation">
		<ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                        To="#FF81C784" Duration="0:0:0.2"/>
	</Storyboard>

	<!-- 4. МЯГКОЕ ИЗМЕНЕНИЕ ЦВЕТА ПРИ ВЫБОРЕ МЕСТА -->
	<Storyboard x:Key="SeatSelectedColorAnimation">
		<ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                        To="#FFFF9800" Duration="0:0:0.3"/>
	</Storyboard>

	<!-- 5. АНИМАЦИЯ ВСПЛЫВАЮЩЕГО ОКНА С ИНФОРМАЦИЕЙ О СЕАНСЕ (появление) -->
	<Storyboard x:Key="PopupSlideInAnimation">
		<DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="0" To="1" Duration="0:0:0.3"/>
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        From="0.7" To="1" Duration="0:0:0.3"/>
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        From="0.7" To="1" Duration="0:0:0.3"/>
	</Storyboard>

	<!-- 6. Анимация закрытия всплывающего окна -->
	<Storyboard x:Key="PopupSlideOutAnimation">
		<DoubleAnimation Storyboard.TargetProperty="Opacity"
                        From="1" To="0" Duration="0:0:0.2"/>
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        From="1" To="0.9" Duration="0:0:0.2"/>
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        From="1" To="0.9" Duration="0:0:0.2"/>
	</Storyboard>

	<!-- 7. Анимация для кнопок -->
	<Storyboard x:Key="ButtonPulseAnimation">
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        From="1.0" To="1.05" Duration="0:0:0.1" AutoReverse="True"/>
		<DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        From="1.0" To="1.05" Duration="0:0:0.1" AutoReverse="True"/>
	</Storyboard>

	<!-- Стиль для мест с анимациями -->
	<Style x:Key="AnimatedSeatButton" TargetType="Button">
		<Setter Property="Cursor" Value="Hand"/>
		<Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
		<Setter Property="RenderTransform">
			<Setter.Value>
				<ScaleTransform ScaleX="1" ScaleY="1"/>
			</Setter.Value>
		</Setter>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Button">
					<Border Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            BorderBrush="#FFE0E0E0"
                            BorderThickness="1">
						<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Style.Triggers>
			<!-- МЯГКОЕ ИЗМЕНЕНИЕ ЦВЕТА ПРИ НАВЕДЕНИИ -->
			<Trigger Property="IsMouseOver" Value="True">
				<Trigger.EnterActions>
					<BeginStoryboard Storyboard="{StaticResource SeatHoverColorAnimation}"/>
					<BeginStoryboard Storyboard="{StaticResource SeatPulseAnimation}"/>
				</Trigger.EnterActions>
				<Trigger.ExitActions>
					<BeginStoryboard>
						<Storyboard>
							<ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                            Duration="0:0:0.15"/>
						</Storyboard>
					</BeginStoryboard>
				</Trigger.ExitActions>
			</Trigger>
		</Style.Triggers>
	</Style>

	<!-- Стиль для анимированных кнопок -->
	<Style x:Key="AnimatedButton" TargetType="Button">
		<Setter Property="Cursor" Value="Hand"/>
		<Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
		<Setter Property="RenderTransform">
			<Setter.Value>
				<ScaleTransform ScaleX="1" ScaleY="1"/>
			</Setter.Value>
		</Setter>
		<Style.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Trigger.EnterActions>
					<BeginStoryboard Storyboard="{StaticResource ButtonPulseAnimation}"/>
				</Trigger.EnterActions>
			</Trigger>
		</Style.Triggers>
	</Style>
</ResourceDictionary>